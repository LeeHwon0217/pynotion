score = 85                   # @note: 정수 객체 85 에 score 라는 이름을 붙인다
if score >= 80:              # @note: 조건이 참이므로 들여쓴 블록으로 들어간다
    print("합격")            # @note: 4칸 들여쓰기 — if 에 속한 줄
    print("축하해요")        # @note: 같은 깊이 — 역시 if 에 속한 줄
print("검사 끝")             # @note: 들여쓰기를 되돌렸다 — if 와 무관하게 항상 실행
