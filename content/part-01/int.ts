import type { Lesson } from "@/lib/types";

export const intLesson: Lesson = {
  part: 1,
  slug: "int",
  blocks: [
    {
      kind: "viz", component: "Story", title: "정수는 끝이 없다",
      props: {
        story: {
          code: ["n = 7", "n = n * 1000", "n = 2 ** 64", "n = 2 ** 200"],
          steps: [
            { chapter: "작은 수", say: "`n = 7`. 정수 객체 하나. 여기까진 평범하다.", ops: [{ line: 0 }, { obj: "a", type: "int", value: "7" }, { bind: "n", to: "a" }] },
            { say: "`n * 1000` — 계산 결과는 **새 객체**다. 7 은 바뀌지 않고, 이름 `n` 이 7000 으로 옮겨 붙는다.", ops: [{ line: 1 }, { obj: "b", type: "int", value: "7000" }, { bind: "n", to: "b" }, { del: "a" }] },
            { chapter: "64비트를 넘어서", say: "다른 언어라면 여기서 멈춘다. 64비트 정수의 최대는 약 1.8 × 10¹⁹ — `2 ** 64` 는 그걸 넘는다.", ops: [{ line: 2 }, { obj: "c", type: "int", value: "18446744073709551616" }, { bind: "n", to: "c" }, { del: "b" }, { badge: "c", text: "20자리 — 문제없음", color: "fresh" }] },
            { say: "파이썬 정수는 **필요한 만큼 자리를 늘린다.** 2 의 200 제곱, 61자리 숫자도 그냥 int 다.", ops: [{ line: 3 }, { obj: "d", type: "int", value: "1606938044258990275541962092341162602522202993782792835301376" }, { bind: "n", to: "d" }, { del: "c" }, { badge: "d", text: "61자리", color: "fresh" }] },
            { chapter: "대가", say: "공짜는 아니다. 큰 정수는 메모리를 더 쓰고 계산도 느리다. 하지만 **오버플로 걱정 없이** 계산할 수 있다는 건 큰 자유다.", ops: [] },
          ],
        },
      },
      caption: "int 는 임의 정밀도(arbitrary precision) 정수다. 팩토리얼, 암호, 큰 수 계산에서 파이썬이 편한 이유.",
    },
    {
      kind: "text",
      md: `\`int\`는 파이썬의 정수 타입이다. 다른 언어와 가장 다른 점 하나 — **크기 제한이 없다.** 64비트를 넘어가면 파이썬이 알아서 자릿수를 늘린다. \`2 ** 1000\`도, \`100!\`도 그냥 계산된다.

만드는 방법:

- 그냥 쓴다: \`42\`, \`-7\`, \`0\`
- 자릿수 구분: \`1_000_000\` (밑줄은 무시된다)
- 다른 진법: \`0b1010\`(2진), \`0o17\`(8진), \`0xff\`(16진) — 전부 같은 \`int\`다
- 문자열에서: \`int("42")\``,
    },
    {
      kind: "trace", traceId: "p1-int-ops", title: "정수 연산 한 줄씩",
      caption: "// 와 % 에서 음수가 어떻게 처리되는지가 함정이다. 파이썬은 '아래로 내림'을 택한다.",
    },
    {
      kind: "heading", text: "// 와 % — 몫과 나머지",
    },
    {
      kind: "table",
      head: ["식", "결과", "설명"],
      rows: [
        ["`17 // 5`", "`3`", "몫. 소수점을 **아래로** 내린다"],
        ["`17 % 5`", "`2`", "나머지"],
        ["`-17 // 5`", "`-4`", "-3.4 를 아래로 내리면 -4 (0 쪽이 아니라 **작은 쪽**)"],
        ["`-17 % 5`", "`3`", "`-17 == 5 * -4 + 3` 이 되도록. 나머지는 **나누는 수와 같은 부호**"],
        ["`divmod(17, 5)`", "`(3, 2)`", "몫과 나머지를 한 번에"],
      ],
      caption: "C나 JavaScript 는 -17 // 5 를 -3 으로 계산한다. 언어마다 다르니 음수 나눗셈은 조심.",
    },
    {
      kind: "pitfall",
      title: "/ 는 항상 float 를 돌려준다",
      md: `\`10 / 2\` 는 \`5\`가 아니라 \`5.0\`이다. 정수 결과가 필요하면 \`//\`를 쓴다. 파이썬 2에서는 \`/\`가 정수 나눗셈이었기 때문에 옛날 자료를 보면 헷갈릴 수 있다.`,
      code: `print(10 / 2)    # 5.0  ← float
print(10 // 2)   # 5    ← int
print(type(10 / 2))  # <class 'float'>`,
    },
    {
      kind: "callout", tone: "deep", title: "왜 크기 제한이 없나",
      md: `CPython 은 정수를 30비트짜리 "자릿수"의 배열로 저장한다. 값이 커지면 배열을 늘린다. 그래서 작은 정수는 28바이트, \`2 ** 200\` 은 52바이트를 차지한다 (\`sys.getsizeof\` 로 확인 가능). 작은 정수 연산이 C보다 느린 이유도 이 구조 때문이다 — 매번 "몇 자리짜리인지" 확인해야 한다.`,
    },
    {
      kind: "quiz",
      question: "`-7 // 2` 의 값은?",
      choices: [
        { text: "`-3`", why: "0 쪽으로 자르면 -3 이지만, 파이썬은 아래로 내린다." },
        { text: "`-4`", correct: true, why: "-3.5 를 아래로 내리면 -4. 파이썬의 // 는 floor(내림) 나눗셈이다." },
        { text: "`-3.5`", why: "// 는 정수 나눗셈이다. 소수가 남지 않는다." },
      ],
    },
    {
      kind: "summary",
      items: [
        "`int` 는 **크기 제한이 없다.** 자릿수는 필요한 만큼 늘어난다.",
        "`/` 는 항상 `float`, `//` 는 내림 나눗셈, `%` 는 나머지.",
        "음수에서 `//` 는 **작은 쪽**으로 내리고, `%` 결과는 나누는 수와 같은 부호.",
        "`0b`, `0o`, `0x` 접두사로 다른 진법을 쓸 수 있고, `1_000_000` 처럼 밑줄로 자릿수를 구분한다.",
      ],
    },
  ],
};
