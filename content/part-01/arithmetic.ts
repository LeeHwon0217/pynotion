import type { Lesson } from "@/lib/types";

export const arithmetic: Lesson = {
  part: 1,
  slug: "arithmetic",
  blocks: [
    {
      kind: "viz", component: "Story", title: "2 + 3 * 4 ** 2 는 어떤 순서로 계산되나",
      props: {
        story: {
          code: ["x = 2 + 3 * 4 ** 2"],
          steps: [
            { chapter: "우선순위", say: "한 줄에 연산자가 셋. 파이썬은 **우선순위** 순으로 계산한다: `**` → `*` → `+`.", ops: [{ line: 0 }] },
            { say: "① `4 ** 2` 가 먼저. 결과 `16` 은 이름 없는 **임시 객체**다.", ops: [{ obj: "t1", type: "int", value: "16", note: "4 ** 2" }] },
            { say: "② 다음 `3 * 16`. 또 임시 객체 `48`. 방금 만든 16 은 더 쓸 데가 없어 사라진다.", ops: [{ obj: "t2", type: "int", value: "48", note: "3 * 16" }, { del: "t1" }] },
            { say: "③ 마지막 `2 + 48`. 이 `50` 에 드디어 이름 `x` 가 붙는다. 중간 결과들은 흔적도 없다.", ops: [{ obj: "t3", type: "int", value: "50", note: "2 + 48" }, { del: "t2" }, { bind: "x", to: "t3" }] },
            { chapter: "괄호", say: "순서를 바꾸고 싶으면 **괄호**. `(2 + 3) * 4 ** 2` 는 `5 * 16 = 80`. 헷갈리면 괄호를 치는 게 정답이다 — 읽는 사람을 위해서도.", ops: [{ badge: "t3", text: "(2 + 3) * 4 ** 2 = 80", color: "name" }] },
          ],
        },
      },
    },
    {
      kind: "text",
      md: `산술 연산자 일곱 개: \`+\`, \`-\`, \`*\`, \`/\`, \`//\`, \`%\`, \`**\`. 우선순위는 수학과 같다.`,
    },
    {
      kind: "table",
      head: ["우선순위", "연산자", "비고"],
      rows: [
        ["높음", "`( )`", "괄호가 항상 먼저"],
        ["", "`**`", "거듭제곱. **오른쪽부터** 묶는다: `2 ** 3 ** 2` = `2 ** 9`"],
        ["", "`+x`, `-x`", "부호. `**` 보다 낮다 — `-2 ** 2` = `-4`"],
        ["", "`*`, `/`, `//`, `%`", "같은 순위끼리는 왼쪽부터"],
        ["낮음", "`+`, `-`", ""],
      ],
    },
    {
      kind: "trace", traceId: "p1-arith", title: "연산자 하나씩 확인",
      caption: "-2 ** 2 가 -4 인 것이 가장 자주 틀리는 부분이다. 부호보다 거듭제곱이 먼저다.",
    },
    {
      kind: "pitfall",
      title: "-2 ** 2 는 4 가 아니다",
      md: `\`**\` 가 부호 \`-\` 보다 우선순위가 높다. 그래서 \`-2 ** 2\` 는 \`-(2 ** 2)\` = \`-4\`. \`(-2) ** 2\` 라고 써야 \`4\`가 나온다. 수학 표기 −2² 와 같은 규칙이지만, 코드로 보면 놀라는 사람이 많다.`,
    },
    {
      kind: "heading", text: "복합 대입 연산자",
    },
    {
      kind: "code",
      code: `count = 0
count += 1        # count = count + 1
count *= 3        # count = count * 3
count -= 2        # count = count - 2
count //= 2       # count = count // 2
print(count)      # 0`,
      caption: "정수에서 += 는 새 객체를 만들어 재바인딩한다. 리스트에서 += 는 객체를 제자리에서 바꾼다 — 이 차이는 Part 3 에서 애니메이션으로 본다.",
    },
    {
      kind: "callout", tone: "tip", title: "정수와 float 가 섞이면",
      md: `\`3 + 1.5\` 처럼 int 와 float 를 섞으면 결과는 **float** 다. 파이썬이 int 를 float 로 승격시킨다. \`/\` 는 두 정수를 나눠도 float 를 돌려준다. \`10 / 5\` → \`2.0\`.`,
    },
    {
      kind: "quiz",
      question: "`2 ** 3 ** 2` 의 값은?",
      choices: [
        { text: "`64`", why: "(2 ** 3) ** 2 = 8 ** 2 = 64 라고 생각하기 쉽지만, ** 는 오른쪽부터 묶는다." },
        { text: "`512`", correct: true, why: "** 는 오른쪽 결합이다: 2 ** (3 ** 2) = 2 ** 9 = 512." },
        { text: "`12`", why: "거듭제곱이지 곱셈이 아니다." },
      ],
    },
    {
      kind: "quiz",
      question: "`10 - 4 - 3` 의 값은?",
      choices: [
        { text: "`3`", correct: true, why: "같은 순위의 - 는 왼쪽부터: (10 - 4) - 3 = 3." },
        { text: "`9`", why: "10 - (4 - 3) = 9 는 오른쪽부터 계산한 것이다. -, +, *, / 는 왼쪽부터 묶는다 (** 만 오른쪽부터)." },
      ],
    },
    {
      kind: "summary",
      items: [
        "우선순위: 괄호 → `**` → 부호 → `* / // %` → `+ -`.",
        "`**` 만 **오른쪽부터** 묶는다. 나머지는 왼쪽부터.",
        "`-2 ** 2` 는 `-4`. 부호보다 거듭제곱이 먼저.",
        "중간 결과는 **이름 없는 임시 객체**다. 마지막 결과에만 이름이 붙는다.",
        "int 와 float 가 섞이면 float. `/` 는 항상 float.",
      ],
    },
  ],
};
