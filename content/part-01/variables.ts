import type { Lesson } from "@/lib/types";

export const variables: Lesson = {
  part: 1,
  slug: "variables",
  blocks: [
    {
      kind: "viz", component: "VariablesScene", title: "변수는 이름표다",
      caption: "재생 버튼을 누르면 시작됩니다. 자막을 따라가며 보세요. 장면 목록에서 원하는 장면부터 다시 볼 수 있고, F 키로 전체화면이 됩니다.",
    },
    {
      kind: "text",
      md: `애니메이션에서 본 것을 말로 정리하면 이렇다.

파이썬에서 \`a = 10\`은 "a라는 상자에 10을 넣는다"가 아니다. **오른쪽의 값 10이 먼저 객체로 만들어지고, 그 객체에 a라는 이름표가 붙는다.** 변수는 값을 소유하지 않는다. 그저 어떤 객체를 가리키는 이름일 뿐이다.

이 한 문장이 파이썬의 거의 모든 "이상한" 동작을 설명한다. 왜 리스트를 복사한 줄 알았는데 같이 바뀌는지, 왜 함수에 리스트를 넘기면 원본이 바뀌는지, 왜 \`is\`와 \`==\`가 다른지 — 전부 여기서 나온다.`,
    },
    {
      kind: "trace", traceId: "p1-variables-basic", title: "한 줄씩 실행해 보기",
      caption: "오른쪽이 메모리다. 이름표(노란색)가 객체(파란 상자)를 가리킨다. a = 20 에서 새 객체가 생기고 a 의 화살표만 옮겨 가는 것을 보라. b 는 그대로다.",
    },
    {
      kind: "heading", text: "'객체'가 정확히 무엇인가",
    },
    {
      kind: "text",
      md: `파이썬에서 존재하는 모든 값은 **객체**다. 숫자 10도, 문자열 "hello"도, 리스트도, 함수도, 심지어 클래스도 객체다. 모든 객체는 세 가지를 갖는다.

- **타입(type)** — 이 객체가 무엇인지. \`type(10)\`은 \`int\`.
- **값(value)** — 객체가 표현하는 내용.
- **아이덴티티(identity)** — 메모리상의 고유한 주소. \`id(10)\`으로 볼 수 있다.

이름(변수)은 이 셋 중 어느 것도 갖지 않는다. 이름은 객체를 가리키는 화살표일 뿐이다. 그래서 파이썬 변수에는 타입이 없다 — 타입은 **객체**에 있다.`,
    },
    {
      kind: "code", title: "직접 확인",
      code: `a = 10
b = a
print(type(a))      # <class 'int'>  ← 타입은 객체의 것
print(id(a) == id(b))  # True  ← 같은 객체를 가리킨다
a = "이제 문자열"      # 이름 a 는 아무 타입이나 가리킬 수 있다
print(type(a))      # <class 'str'>`,
    },
    {
      kind: "heading", text: "재바인딩 vs 객체 변경 — 반드시 구분해야 하는 두 가지",
    },
    {
      kind: "text",
      md: `변수와 관련해 일어나는 일은 정확히 두 종류뿐이다.

1. **재바인딩(rebinding)** — \`a = 다른것\`. 이름표를 떼어 **다른 객체**에 붙인다. 원래 객체는 건드리지 않는다.
2. **객체 변경(mutation)** — \`lst.append(3)\`, \`d["k"] = v\`. 이름표는 그대로 두고 **객체의 내용**을 바꾼다.

정수·문자열·튜플은 **바꿀 수 없는(immutable)** 객체라 2번이 아예 불가능하다. 그래서 정수만 다룰 때는 이름표 모델을 몰라도 티가 안 난다. 문제는 리스트·딕셔너리·집합처럼 **바꿀 수 있는(mutable)** 객체를 만나는 순간 터진다.`,
    },
    {
      kind: "trace", traceId: "p1-variables-alias", title: "리스트에서 무슨 일이 일어나나",
      caption: "other.append(3) 은 화살표를 옮기지 않는다. 객체 자체를 바꾼다. 그래서 같은 객체를 가리키는 nums 도 3 을 본다. 반면 list(nums) 는 진짜로 새 객체를 만든다.",
    },
    {
      kind: "pitfall",
      title: "복사한 줄 알았는데 원본이 바뀌었다",
      md: `\`other = nums\` 는 복사가 아니다. 파이썬에 "값을 복사하는 대입"은 없다. \`=\`는 언제나 이름 붙이기다.

진짜 복사본이 필요하면 **명시적으로 새 객체를 만들어야** 한다: \`list(nums)\`, \`nums[:]\`, \`nums.copy()\`, 중첩 구조라면 \`copy.deepcopy(nums)\`. (얕은/깊은 복사의 차이는 Part 3에서 애니메이션으로 다룬다.)`,
    },
    {
      kind: "callout", tone: "deep", title: "더 깊이: 이름은 어디에 저장되나",
      md: `이름표들은 **네임스페이스**라는 딕셔너리에 산다. 모듈 수준의 이름은 \`globals()\`, 함수 안의 이름은 \`locals()\`로 볼 수 있다. \`a = 10\`은 사실상 \`globals()["a"] = <10 객체>\`와 같다. 이름 → 객체 매핑이 문자 그대로 딕셔너리인 것이다. 함수 스코프(Part 4)에서 이 사실이 다시 등장한다.`,
    },
    {
      kind: "trace", traceId: "p1-variables-swap", title: "보너스: 값 교환 x, y = y, x",
      caption: "임시 변수 없이 교환이 되는 이유 — 오른쪽 (y, x) 튜플이 먼저 만들어지고, 그 다음 이름이 다시 붙기 때문이다. 값이 이동하는 게 아니다.",
    },
    {
      kind: "try",
      title: "직접 해보기",
      starter: `a = [1, 2, 3]
b = a
b.append(4)
print(a)        # 무엇이 나올까?

c = a[:]        # 슬라이스로 복사
c.append(5)
print(a, c)     # a 는 바뀌었을까?`,
      hint: "머릿속으로 화살표를 그려보자. b 와 a 는 같은 객체, c 는 새 객체.",
    },
    {
      kind: "quiz",
      question: "다음 코드의 출력은?",
      code: `x = 5
y = x
x = x + 1
print(x, y)`,
      choices: [
        { text: "`6 6`", why: "x = x + 1 은 새 객체 6 을 만들어 x 에만 붙인다. y 는 여전히 5 를 가리킨다." },
        { text: "`6 5`", correct: true, why: "정수는 바꿀 수 없으므로 x + 1 은 새 객체를 만들고, 이름 x 만 옮겨 붙는다(재바인딩). y 는 영향 없음." },
        { text: "`5 5`", why: "x 는 6 으로 재바인딩되었다." },
      ],
    },
    {
      kind: "quiz",
      question: "다음 코드의 출력은?",
      code: `a = [1, 2]
b = a
a = a + [3]
b.append(4)
print(a, b)`,
      choices: [
        { text: "`[1, 2, 3, 4] [1, 2, 3, 4]`", why: "a + [3] 은 새 리스트를 만든다 (재바인딩). 그 순간 a 와 b 는 서로 다른 객체가 된다." },
        { text: "`[1, 2, 3] [1, 2, 4]`", correct: true, why: "a = a + [3] 은 **새 객체**를 만들어 a 에 붙인다. b 는 원래 객체 [1, 2] 를 그대로 가리키고, 거기에 4 를 append 한다." },
        { text: "`[1, 2, 3] [1, 2, 3, 4]`", why: "a + [3] 은 원래 객체를 바꾸지 않는다. b 는 [1, 2] 를 보고 있다." },
      ],
      explain: "핵심: `a = a + [3]` 은 **재바인딩**, `b.append(4)` 는 **객체 변경**. 둘은 완전히 다른 일이다. 참고로 `a += [3]` 이었다면 리스트에서는 객체 변경으로 동작해 결과가 달라진다 — 그 이야기는 Part 3에서.",
    },
    {
      kind: "summary",
      items: [
        "`=` 는 값을 복사하지 않는다. 오른쪽 객체에 왼쪽 **이름표를 붙이는** 것이다.",
        "타입·값·id 는 **객체**의 것이다. 변수(이름)는 아무것도 갖지 않는다.",
        "**재바인딩**(이름을 다른 객체로) 과 **객체 변경**(append 등) 을 구분하라.",
        "바꿀 수 있는 객체(리스트·딕셔너리)를 같이 가리키는 이름이 있으면, 한쪽에서 바꾼 것을 다른 쪽도 본다.",
        "진짜 복사는 `list(x)`, `x[:]`, `x.copy()`, `copy.deepcopy(x)` 처럼 **새 객체를 만들어야** 한다.",
      ],
    },
  ],
};
